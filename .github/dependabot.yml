version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "deps"
    open-pull-requests-limit: 10
    allow:
      - dependency-type: "all"
    groups:
      # Bundle all patch and minor updates
      patch-and-minor-updates:
        update-types:
          - "patch"
          - "minor"
      
      # Vitest testing framework
      vitest:
        patterns:
          - "vitest"
          - "@vitest/*"
      
      # ESLint and plugins
      eslint:
        patterns:
          - "eslint-plugin-*"
          - "eslint-config-*"
          - "eslint"
      
      # Commitlint and configs
      commitlint:
        patterns:
          - "@commitlint/*"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
      day: "monday"
      time: "09:00"
    labels:
      - "dependencies"
    commit-message:
      prefix: "chore"
      include: "scope"
    open-pull-requests-limit: 5
    reviewers:
      - "manikandan-ravikumar"